/*const dropdown = {
    init: function (dropElem) {
        dropElem.addEventListener("click", dropdown.hendlerToggle(e))
    },

    hendlerToogle: function (e) {
        var id = e.target.getAttribute('id');
        e.classList.toggle('.dropdown_open')
    }
}*/

//dropdown.init(document.getElementsByClassName('.dropdown'))